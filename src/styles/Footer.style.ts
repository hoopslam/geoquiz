import styled from "styled-components";

export const FooterWrapper = styled.footer`
  .attribution {
    p {
      font-size: 0.9rem;
      margin: 10px;
    }
    a:visited,
    a:link {
      color: grey;
    }
  }
`;
